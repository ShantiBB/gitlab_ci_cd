stages:
  - test

build:
  stage: test
  tags:
    - shell
  script:
    - bash -c "echo 'Start test'"
    - bash -c "sleep 5"
    - bash -c "echo 'Test complete'"
    - bash -c "echo 'Hello MotherFucker :)'"
