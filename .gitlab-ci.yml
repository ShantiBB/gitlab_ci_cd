stages:
  - deploy

deploy:
  stage: deploy
  tags: [shell]
  script:
    - sudo bash -c "docker compose -f "$DOCKER_COMPOSE_PATH" down"
    - cd ansible
    - echo "$ANSIBLE_VAULT_PASSWORD" | ansible-playbook playbook.yml -ask-vault-pass
  only:
    - main
